# AI 需求文档

## 记录
- 恢复预览区域结构：移除错误的导出裁切包裹层，保持原有手机框架与布局不变。
- 导出逻辑恢复为基于 `wechat-screen` 截图。
- 调整导出裁切区域：新增 `wechat-capture` 包裹群名与聊天内容，导出仅截该区域，排除状态栏与输入框。
- 修正语料库类型推断：显式标注自定义对话/系统数组类型，避免 concat 类型不匹配。
- 调整语料库合并逻辑：使用 map 泛型与展开数组，避免 concat 类型推断失败。
- 恢复预览布局并确保输入框显示：移除导出包裹层，新增 `wechat-titlebar`/`wechat-input-bar` ID，仅用于导出裁切。
- 导出裁切逻辑改为计算 header 与 input bar 的矩形范围，截图仅包含群名+聊天内容。
- 将 UI 设置折叠为“高级设置”，默认收起以减少面板占用空间。
- 基础设置保留群名与人数，其他 UI 设置移入“高级设置”并默认折叠。
- 基础设置新增模式切换与预览主题选择，导出区仅保留导出相关控件。
- 修正导出裁切坐标：引入屏幕缩放比，避免预览缩放导致裁切高度为 0 或错位。
- 批量导出改为 ZIP：引入 JSZip，将多张截图打包为单个 zip 下载，避免多文件下载拦截。
- 将“单张内容量”移入高级设置，导出设置仅保留导出相关项。
- 导出张数改为数字输入框，移除滑块样式。
- 导出裁切改用 offsetTop 计算，避免缩放导致裁切高度为 0 从而生成空 zip。
- 批量导出增加进度提示：显示已导出张数/总张数。
- 新增“下载当前 PNG”快捷导出：直接按预览区内容导出单张 PNG。
- 快捷下载改用 Blob 方式触发，兼容性更好；刷新按钮文案简化为“刷新”。
- 导出失败增加显式提示与统一下载触发函数，避免无响应。
- 新增全局 Toast 提示：导出成功/失败使用自定义提示替代系统弹窗。
- 导出逻辑禁用 allowTaint，避免跨域资源导致导出失败；错误提示显示真实异常信息。
- html2canvas 开启 foreignObjectRendering，避免 oklch 颜色解析失败导致导出异常。
- 调整 CloudBase 初始化类型：补充 `CloudbaseInitConfig` 并移除 `as any`，避免类型绕过。
- 移动端隐藏音乐播放器，避免遮挡小屏内容。

## [2026-01-12] 移动端交互与预览深度优化

### 1. 响应式布局优化
- **DeviceFrame.vue**: 针对移动端隐藏了物理手机外壳、阴影和装饰性按键，实现了全屏原生级微信预览。
- **全屏填充**: 在移动端模式下自动移除圆角和固定边距，最大化视口利用率。

### 2. 移动端编辑模式交互
- **GeneratorView.vue**: 引入了移动端专用的浮动操作按钮 (FAB)。
- **底部抽屉 (Bottom Sheet)**: 实现了移动端编辑面板的抽屉式弹出效果（85vh 高度 + 背景模糊），解决了移动端无法操作配置的问题。

### 3. AI 基础设施同步
- 在 `AI_Common` 目录下建立了全套 `gemini` 和 `codex` 的技能/MCP 指南文档。
- 优化了 `juejin-writer` 技能，支持 SVG 逻辑图生成、封面图下载及颜文字梗。